import { cmslibBackendRoutes } from './-cmslib-backend-routes';

describe('cmslibBackendRoutes', () => {
  it('should work', () => {
    expect(cmslibBackendRoutes()).toEqual('-cmslib-backend-routes');
  });
});
